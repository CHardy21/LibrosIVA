from config.general_config import *

import gui.mng_gui as gui
import os

# ruta_relativa = "proyecto/archivo.txt"
# ruta_absoluta = os.path.abspath(ruta_relativa)
# print(ruta_absoluta)

login = gui.Login()
